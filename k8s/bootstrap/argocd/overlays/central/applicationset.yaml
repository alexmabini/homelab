apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: central-applications
  namespace: argocd
spec:
  generators:
    - git:
        repoURL: https://github.com/alexmabini/homelab.git
        revision: HEAD
        directories:
          - path: k8s/bootstrap/**/overlay/central-test/*
  template:
    metadata:
      name: "{{path.basename}}"
    spec:
      project: bootstrap
      source:
        repoURL: https://github.com/alexmabini/homelab.git
        targetRevision: HEAD
        path: "{{path}}"
      destination:
        server: https://kubernetes.default.svc
        namespace: "{{path.basename}}"
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
